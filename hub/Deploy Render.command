#!/bin/bash
# เปิดหน้า Deploy Property Hub บน Render (ครั้งแรกต้อง Sign in with GitHub แล้วกด Apply)
open "https://dashboard.render.com/blueprint/new?repo=https://github.com/mycondobkkhost-cmd/pantip-property-hub"
echo "เปิด Render Blueprint ในเบราว์เซอร์แล้ว"
echo "1) Sign in with GitHub"
echo "2) กด Apply"
echo "3) เพิ่ม HUB_USERS_JSON ใน Environment (เปลี่ยนรหัสผ่าน)"
echo "4) รอ build ~5-10 นาที"
