# Phase Z4 Owner Master Definition Review

## Scope

Owner reviews **2 master definitions** only:
1. Pattanakarn (พัฒนาการ)
2. Rama 9 (พระราม 9)

Suan Luang deferred (INSUFFICIENT_EVIDENCE).

## Local review UI

```bash
bash scripts/start_master_definition_review.sh
# → http://127.0.0.1:8765/master-definition-review/
```

Uses `HUB_SKIP_PREVIEW_BOOT=1` to avoid 8.3MB preview-data.js rebuild.

## Status

All topics remain `REVIEW_REQUIRED` until owner explicitly decides. No auto-approval.
